# NetDevil Common Formats

Shared data formats used across multiple systems:

| Directory | Format | Description |
|-----------|--------|-------------|
| [ldf/](ldf/) | LDF | Key-value config (text `key=type:value` and binary `wstr8` variants) |
